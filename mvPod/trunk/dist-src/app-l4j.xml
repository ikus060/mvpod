<launch4jConfig>
  <outfile>bin/${app.file.name}.exe</outfile>
  <dontWrapJar>true</dontWrapJar>
  <headerType>gui</headerType>
  <errTitle>${app.name}</errTitle>
  <cmdLine></cmdLine>
  <chdir>../</chdir>
  <priority>normal</priority>
  <downloadUrl>http://java.com/download</downloadUrl>
  <supportUrl></supportUrl>
  <customProcName>true</customProcName>
  <stayAlive>false</stayAlive>
  <manifest></manifest>
  <icon>./bin/app-icon.ico</icon>
  <classPath>
    <mainClass>${main.class}</mainClass>
    <cp>${dist.win32.classpath}</cp>
  </classPath>
  <jre>
    <path></path>
    <minVersion>1.6.0</minVersion>
    <maxVersion></maxVersion>
    <jdkPreference>preferJre</jdkPreference>
  </jre>
  <versionInfo>
    <fileVersion>${version}</fileVersion>
    <txtFileVersion>${version.text}</txtFileVersion>
    <fileDescription>${app.description}</fileDescription>
    <copyright>Copyright 2008, Patrik Dufresne</copyright>
    <productVersion>${version}</productVersion>
    <txtProductVersion>${version.text}</txtProductVersion>
    <productName>${app.name}</productName>
    <companyName>TODO, inc.</companyName>
    <internalName>${app.name}</internalName>
    <originalFilename>${app.file.name}.exe</originalFilename>
  </versionInfo>
  <messages>
    <startupErr>An error occurred while starting the application.</startupErr>
    <bundledJreErr>This application was configured to use a bundled Java Runtime Environment but the runtime is missing or corrupted.</bundledJreErr>
    <jreVersionErr>This application requires a Java Runtime Environment</jreVersionErr>
    <launcherErr>The registry refers to a nonexistent Java Runtime Environment installation or the runtime is corrupted.</launcherErr>
  </messages>
</launch4jConfig>
